import PropTypes from 'prop-types';
declare const Initializer: {
    ({ updatePlugin }: any): null;
    propTypes: {
        updatePlugin: PropTypes.Validator<(...args: any[]) => any>;
    };
};
export default Initializer;
