const pluginId = require('./../../admin/src/pluginId');

const PLUGIN_NAME = pluginId;
const CONFIG_NAME = 'mux';
const CONFIG_KEY = 'config';

module.exports = {
  PLUGIN_NAME,
  CONFIG_NAME,
  CONFIG_KEY
};
